<!-- production-harness-v2:start -->
Use the smallest safe process that produces evidence. The durable daily route is Luna Xhigh root-direct for S0-S2; the actual Human- or platform-selected root remains authoritative. Record expected and actual model, effort, role, and direct/delegated topology when the platform exposes them. The normal Human loop is requirement, one GO for route/acceptance/authorization, and final evidence review.

Classify S0-S4 before editing:
- S0 discussion/read-only: Luna Xhigh root direct; no routine Skill, child, review, Packet, CodeGraph, browser, or Capsule.
- S1 narrow change: Luna Xhigh root direct and one deterministic focused check. No child on first-pass success; first-check failure, narrow repair, or boundary permits one Luna Medium luna_qa pass.
- S2 bounded work: Luna Xhigh root implements and checks, then one compact read-only Luna Medium luna_qa pass. This is same-family QA, never independent verification.
- S3 hidden acceptance or boundary risk: root freezes scope, Terra High terra_worker implements, then Luna Medium luna_verifier independently reviews.
- S4 milestone work: root repeats the S3 loop one milestone at a time.

Named child roles have no manual model override. A child verdict is accepted only after exposed actual identity matches its role contract; record a mismatch and rerun the correct role once. If a root is off-route, finish safely and correct at the next natural boundary; never claim compliance or create a replacement child solely for identity. Keep max_threads=2, max_depth=1, one active child at a time, no parallel writers, and no automatic ultra/max.

Before completion, report changed files, relevant checks, skipped checks with reason, and residual risk. Keep strict boundaries for secrets, destructive actions, database mutation, deployment/restart, and unapproved remote mutation.
<!-- production-harness-v2:end -->