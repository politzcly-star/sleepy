# Changelog

## v1.0.34 (versionCode 35)

**颜色架构拆分** — 「统一课程底色」开关拆成两个独立的:

- 小组件开关 (`widget_colorless`): 只管桌面小组件, 行为不变。
- 课程胶囊开关 (`course_colorless`): 只管 App 里的课表/今日页, 新增, 默认关。

之前两件事绑一个开关, 改 A 就会影响 B, 用户怨声载道。现在各管各的, 默认恢复彩色, 互不打扰。

**修复**:

- 云备份文件名 `settings.xml` 改成 `sleepy_prefs.xml` — 之前根本没覆盖过偏好, 白备份了三年。
- about 页的「可改用镜像下载」改成「自动回退镜像」, 之前语义反了。
- 删课表时, 相关的课前闹钟不再跟着一起变成孤魂野鬼。

**设置页改动**:

- 设置入口重排: 底部导航从原来乱糟糟的 8 项缩成 5 项, 主题/显示/小组件各自独立成卡。
- 「跟随系统」标签拆成两个: 浅色跟随系统 / 深色跟随系统, 各管各的。

**多语言**:

- zh-rTW 简体残留扫光, 两岸用词统一 (檔案/匯出/文字)。
- ja 的「授業」和 es 的「Período」统一化。
- en/ja/es/zh-rTW 主题 key 漏翻的 16 处全补上。

**inWeek 语义修正**:

- 之前单双周课程会被强行扩成「起止周」显示, 其实根本不是起止周。这次改成按实际周次来。**课表突然少了几格, 不是丢课了, 是之前显示错了。**

**稳定性**:

- lintRelease 8 项 error 全部清零: 通知权限校验守卫, widget 尺寸读取的 API31/32 兼容, ExportScreen 的 FileProvider 回退。
- 提醒子系统里 `cancelAll` 从 `runBlocking` 改成协程, 界面不再卡。
- 分钟输入加了 debounce, 疯狂点不会炸。

**ICS 导出**:

- 单双周课程现在正确导出 `INTERVAL=2`, 之前当普通课导了。
- 自定义时间也能导出了。

**代码清理** (用户不可见):

- Glance 框架整层删掉, 全部走 RemoteViews。
- 死代码清了一百多处, APK 体积略微缩小。

**既知问题 (BG-14)**:

- 新装用户的小组件没有 15 分钟兜底刷新 (`WidgetUpdater.schedule` 死链), v1.0.35 修。想手动刷的话, 进 App 随便改点啥就行。
