# Changelog

## v1.0.34 (versionCode 35)

**颜色架构拆分** — 将「统一课程底色」开关拆分为两个独立开关:

- 小组件开关 (`widget_colorless`, A): 只控制桌面小组件的课程底色, 行为不变。
- 课程胶囊开关 (`course_colorless`, B, 新增默认关闭): 只控制 App 内课表/今日页课程胶囊的底色。

拆分后默认状态下 App 课程胶囊恢复彩色, 小组件与 App 两侧互不影响, 各自独立开关。

**修复**:

- 云备份规则文件名修正 — `settings.xml` → `sleepy_prefs.xml`(自 v1.0.0 起云备份/设备迁移从未覆盖任何偏好)。
- about 更新说明文案由「可改用镜像下载」修正为「自动回退镜像」语义。

**语义变更说明 (inWeek)**:

- 起止周不再被强制显示。此前单双周课程会被错误地扩成连续起止周显示, 本次修复后按实际周次显示; **部分课表「少了几格」是修复而非丢课**, 属预期行为。

**既知问题 (BG-14)**:

- 新安装用户的小组件暂无 15 分钟兜底自动刷新(WidgetUpdater.schedule 死链), 已在 v1.0.35 排期修复。若桌面小组件未及时更新, 请手动进 App 触发一次数据变更。
